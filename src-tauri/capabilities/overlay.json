{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "El overlay escucha eventos de dominio, lee settings y permite arrastrar su ventana.",
  "windows": ["overlay"],
  "permissions": ["core:event:default", "core:window:allow-start-dragging"]
}
