{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "El overlay solo escucha eventos de dominio (render puro, sin comandos).",
  "windows": ["overlay"],
  "permissions": ["core:event:default"]
}
